--//config//--

local addon, ns = ...

ns.cfg = {
	["current_mode"] = true,					-- Automatically switches to current fight data whenever a new fight starts, and back to overall data when it ends
	["real_value"] = false,						-- Substracts overdamage; only real damage dealt is shown
}